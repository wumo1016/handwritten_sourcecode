import install from './install'

class VueRouter {
  constructor(options) {
    console.log(options)
  }

  init(app) {
    // console.log(app)
  }
}

VueRouter.install = install

export default VueRouter
